#!/usr/bin/python3

var = lambda x: x*2
print (var(2))

delta = lambda a,b,c: ((b ** 2) - (4 * a * c))
print (delta(3,-2,-5))

calculadora = lambda x,y: x * y

x = 1
y = 1 
for x <=10:
    x = x =1
    for y < 10:
        print ("%s x %s = %s" % (x, y, calculadora(x,y)))
        y = y + 1
        